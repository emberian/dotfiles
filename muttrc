source ~/.mutt/basic.rc
source ~/.mutt/theme.rc
source ~/.mutt/pgp.rc

# Archive when hitting the delete key (from Gary Bernhardt's config)
macro index y "<save-message>+Archive<enter>"
macro pager y "<save-message>+Archive<enter>"
